import { describe, it, expect } from 'vitest';
import { ArtistCardComponent } from './artist-card.component';

describe('ArtistCardComponent', () => {
  it('should create', () => {
    expect(ArtistCardComponent).toBeDefined();
  });
});