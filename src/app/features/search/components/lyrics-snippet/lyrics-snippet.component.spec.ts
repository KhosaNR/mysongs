import { describe, it, expect } from 'vitest';
import { LyricsSnippetComponent } from './lyrics-snippet.component';

describe('LyricsSnippetComponent', () => {
  it('should create', () => {
    expect(LyricsSnippetComponent).toBeDefined();
  });
});